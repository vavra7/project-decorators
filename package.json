{
  "name": "project-decorators",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently 'cd packages/server && yarn dev'"
  },
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
