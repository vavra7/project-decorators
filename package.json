{
  "name": "project-decorators",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently 'cd packages/express-decorators && yarn dev' 'cd packages/type-express && yarn dev' 'cd packages/api && yarn dev' 'cd packages/client && yarn dev'"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
